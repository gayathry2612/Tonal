# ── Runtime dependencies ──────────────────────────────────────────────
# Qt GUI framework + multimedia (audio engine built-in via AVFoundation/GStreamer/WMF)
PySide6>=6.6.0

# Audio metadata (MP3/FLAC/OGG/M4A/WAV tags, album art extraction)
mutagen>=1.47.0

# Image processing for album art display
Pillow>=10.0.0

# ── Development / packaging (not required by end users) ──────────────
# Standalone app bundler
PyInstaller>=6.3.0
